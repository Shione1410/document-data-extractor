# Changelog

このファイルには Document Data Extractor の主な変更履歴を記録します。

形式は Keep a Changelog の考え方を参考にしています。

## [1.20.15] - 2026-09-23

### Added
- OCRパイプライン設定 `OCR.yaml` の明示読み込みに対応。
- PyInstaller実行環境でもOCR設定ファイルを解決できる `resource_path()` を追加。
- OCR設定ファイルの実パスをログへ表示。
- PyInstaller向けにPaddleX依存パッケージmetadataを収集するビルド構成を整備。

### Changed
- 結果画面の「信頼度」を「OCR信頼度」へ変更。
- Excel / CSV / Word / テキストPDFなど直接抽出データではOCR信頼度を `―` 表示に変更。
- exe配布形式を `onedir` 前提に整理。

### Fixed
- exe版で `The pipeline (OCR) does not exist!` となる問題へ対応。
- exe版で `OCR requires additional dependencies` となるPaddleX依存判定問題へ対応。
- Paddleを丸ごと収集した際にWindowsの深いパスでPyInstaller COLLECTが失敗する問題を回避。
- exe版でOCR必須サンプルPDFの抽出まで動作確認。

## [1.20.14]

### Changed
- 解析結果画面を簡略化。
- 通常表示を「ファイル / 修正後文字列 / OCR信頼度 / 確認」に整理。
- ページ/シート、抽出種別、OCR元文字列、判定、判定元を「詳細情報を表示」へ移動。
- 抽出種別を日常利用上の主要情報ではなく、監査・デバッグ向け内部情報として整理。

## [1.20.13]

### Changed
- PDF等の内部抽出種別 `word` の画面表示を「文字列」へ変更。

## [1.20.12]

### Changed
- 出力列の初期選択を「ファイル」「修正後文字列」のみに変更。
- 内部情報や補助列は必要に応じて利用者が選択する方式へ変更。

## [1.20.11]

### Added
- CSV入力に対応。
- `UTF-8-SIG`、`UTF-8`、`CP932` の文字コードを順に試行。
- CSVセルを行・列情報付きレコードとして抽出。

## [1.20.10]

### Changed
- 保存ダイアログ等の表示を日本語化。

## [1.20]

### Added
- 公開配布を意識したGUI整理。
- 右側のログ / 進捗ペイン。
- ログ表示 / 非表示切り替え。
- データ品質レベル表示。
- Bronze / Silver Candidate の状態表示。
- 解析結果タブの縦スクロール。
- 統一スクロールバーデザイン。

### Changed
- ファイル操作、横持ち整列、結果操作のUIを整理。
- Silver Verifiedはアプリが自動認定しない方針を明示。

## [1.19]

### Added
- OCR断片のスマート結合。
- 距離、文字種、型番パターン、Key / Valueらしさを利用した結合判定。

### Changed
- 日本語文字列、英数字・ハイフン型番、数値＋単位などの結合精度を改善。
- Key＋数値など、本来分離したい組み合わせを結合しにくく調整。

## [1.18]

### Added
- OCRトークン結合処理。
- ハイフン類の表記正規化。

### Changed
- OCR元文字列を保持したまま、修正後文字列側で結合・正規化を行う方式へ整理。

## [1.17]

### Added
- PDF / OCR結果の適応型読み順補正。
- 文字ボックス高さ、縦方向重なり、中心位置を用いた行クラスタリング。

### Changed
- 行を上→下、同一行を左→右に並べる処理を改善。

## [1.16]

### Added
- キーワードアンカーによる横持ち整列。
- ファイル間で特定キーワードの位置を揃える機能。

## [1.15]

### Fixed
- 自動分類実行時のGUIフリーズを改善。
- Worker計算 → 結果受信 → Thread終了 → GUI分割反映の順序へ変更。
- QThreadのライフサイクルを整理。
